name: Docker Image CI

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: This workflow has moved
        run: echo "GHCR publishing is handled by docker-publish.yml"
